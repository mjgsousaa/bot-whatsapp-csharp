<Window x:Class="BotWhatsappCSharp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - BotZap AI" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" Background="White" ResizeMode="NoResize">
    <Grid>
        <Border Padding="40" VerticalAlignment="Center">
            <StackPanel>
                <Image Source="/Assets/SidebarLogo.png" Height="55" Margin="0,0,0,40" Stretch="Uniform"/>
            
            <TextBlock Text="Bem-vindo de volta" FontSize="20" FontWeight="Bold" Margin="0,0,0,5" HorizontalAlignment="Center" Foreground="#333"/>

            <TextBox x:Name="KeyEntry" Height="45" VerticalContentAlignment="Center"
                     Padding="10,0" Margin="0,0,0,20" FontSize="14"
                     Tag="Cole sua chave de licença..."/>

            <Button x:Name="LoginBtn" Content="ENTRAR NO SISTEMA" Height="45"
                    Background="#2d3436" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Margin="0,0,0,20" Click="LoginBtn_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="⚠️ Ler Termos de Uso e Riscos" Height="30" Background="Transparent" BorderThickness="0" Foreground="#d63031" FontWeight="SemiBold" Cursor="Hand" Click="TermsBtn_Click"/>

            <TextBlock x:Name="MsgLabel" Text="Insira sua chave para continuar." 
                       HorizontalAlignment="Center" FontSize="14" Foreground="#2d3436"/>
        </StackPanel>
        </Border>
    </Grid>
</Window>
